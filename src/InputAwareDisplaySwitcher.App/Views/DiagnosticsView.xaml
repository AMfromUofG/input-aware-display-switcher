<UserControl x:Class="InputAwareDisplaySwitcher.App.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Margin="0,0,0,12"
               Foreground="#FF4B5563"
               TextWrapping="Wrap"
               Text="The diagnostics history is already backed by structured records so this page can evolve into a live troubleshooting panel without replacing the underlying logging model." />

    <DataGrid Grid.Row="1"
              ItemsSource="{Binding Records}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Timestamp"
                            Binding="{Binding TimestampLocal}"
                            Width="150" />
        <DataGridTextColumn Header="Severity"
                            Binding="{Binding Severity}"
                            Width="90" />
        <DataGridTextColumn Header="Category"
                            Binding="{Binding Category}"
                            Width="110" />
        <DataGridTextColumn Header="Event"
                            Binding="{Binding EventType}"
                            Width="180" />
        <DataGridTextColumn Header="Message"
                            Binding="{Binding Message}"
                            Width="2*" />
        <DataGridTextColumn Header="Details"
                            Binding="{Binding Details}"
                            Width="2*" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
