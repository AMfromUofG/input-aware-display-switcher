# Workflows

This folder is reserved for future GitHub Actions workflows.

CI has not been introduced yet because the repository is still in setup and feasibility mode. When automated workflows are added, they should stay simple, useful, and aligned with the current maturity of the project.
