<Window x:Class="RawInputPrototype.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Raw Input Device Identity Feasibility Prototype"
        Width="1680"
        Height="960"
        MinWidth="1100"
        MinHeight="700">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="3*" />
      <RowDefinition Height="2*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0"
                Margin="0,0,0,12">
      <TextBlock FontSize="22"
                 FontWeight="SemiBold"
                 Text="Windows Raw Input Feasibility Harness" />
      <TextBlock Margin="0,6,0,0"
                 TextWrapping="Wrap">
        This prototype listens for Raw Input keyboard and mouse events, shows which device produced each event,
        and keeps a richer device snapshot so you can compare session-scoped handles, device paths, SetupAPI metadata,
        and candidate persistence keys while testing reconnect and restart behaviour.
      </TextBlock>
    </StackPanel>

    <DockPanel Grid.Row="1"
               Margin="0,0,0,12"
               LastChildFill="False">
      <TextBlock x:Name="StatusTextBlock"
                 VerticalAlignment="Center"
                 TextWrapping="Wrap" />
      <StackPanel DockPanel.Dock="Right"
                  Orientation="Horizontal">
        <Button Margin="12,0,0,0"
                Padding="12,6"
                Click="RefreshDevicesButton_Click"
                Content="Refresh Devices" />
        <Button Margin="12,0,0,0"
                Padding="12,6"
                Click="CopySnapshotButton_Click"
                Content="Copy Device Snapshot" />
        <Button Margin="12,0,0,0"
                Padding="12,6"
                Click="CopyLogButton_Click"
                Content="Copy Event Log" />
        <Button Margin="12,0,0,0"
                Padding="12,6"
                Click="ClearLogButton_Click"
                Content="Clear Log" />
      </StackPanel>
    </DockPanel>

    <GroupBox Grid.Row="2"
              Header="Detected input devices (snapshot)"
              Margin="0,0,0,12">
      <Grid Margin="8">
        <Grid.RowDefinitions>
          <RowDefinition Height="3*" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="DevicesDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  EnableRowVirtualization="True"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  SelectionChanged="DevicesDataGrid_SelectionChanged"
                  SelectionMode="Single">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Type"
                                Binding="{Binding DeviceTypeText}"
                                Width="90" />
            <DataGridTextColumn Header="Handle"
                                Binding="{Binding DeviceHandleText}"
                                Width="155" />
            <DataGridTextColumn Header="Friendly Name"
                                Binding="{Binding FriendlyNameText}"
                                Width="230" />
            <DataGridTextColumn Header="Candidate Key"
                                Binding="{Binding CandidatePersistenceKey}"
                                Width="280" />
            <DataGridTextColumn Header="Instance ID"
                                Binding="{Binding DeviceInstanceIdText}"
                                Width="320" />
            <DataGridTextColumn Header="VID"
                                Binding="{Binding VendorId}"
                                Width="80" />
            <DataGridTextColumn Header="PID"
                                Binding="{Binding ProductId}"
                                Width="80" />
            <DataGridTextColumn Header="Raw Device Path"
                                Binding="{Binding DeviceName}"
                                Width="*" />
          </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="1"
                   Margin="0,10,0,6"
                   FontWeight="SemiBold"
                   Text="Selected device identity analysis" />

        <TextBox x:Name="SelectedDeviceAnalysisTextBox"
                 Grid.Row="2"
                 FontFamily="Consolas"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 AcceptsReturn="True" />
      </Grid>
    </GroupBox>

    <GroupBox Grid.Row="3"
              Header="Live raw input event log">
      <DataGrid x:Name="EventsDataGrid"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                EnableRowVirtualization="True"
                IsReadOnly="True"
                HeadersVisibility="Column"
                SelectionMode="Single">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Timestamp"
                              Binding="{Binding TimestampText}"
                              Width="170" />
          <DataGridTextColumn Header="Type"
                              Binding="{Binding EventType}"
                              Width="95" />
          <DataGridTextColumn Header="Source"
                              Binding="{Binding InputSource}"
                              Width="100" />
          <DataGridTextColumn Header="Handle"
                              Binding="{Binding DeviceHandle}"
                              Width="160" />
          <DataGridTextColumn Header="Device"
                              Binding="{Binding DeviceLabel}"
                              Width="260" />
          <DataGridTextColumn Header="Summary"
                              Binding="{Binding Summary}"
                              Width="*" />
        </DataGrid.Columns>
      </DataGrid>
    </GroupBox>
  </Grid>
</Window>
